# TODO

## Workflows

- [ ] Pull Docker images from container registry in `deploy.yaml`
- [ ] Add health check to `deploy.yaml` workflow

## Onboarding

- [ ] Move `ppp-example-python` code into the monorepo 
- [ ] Use `docker-compose` layering to configure the service